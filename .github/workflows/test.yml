name: Minikube Runner Test

on:
  push:

jobs:
  test:
    runs-on: [self-hosted, minikube, k8s-runner, runner-deployment-xwllc-9d85n]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Say Hello
        run: echo "Hello from Minikube!"
